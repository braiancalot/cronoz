// Space every page keeps clear under its header, where the toast lands. Sized
// to the 56px toast plus clearance; shrinking it puts toasts back over content,
// and a tap that dismisses one then falls through to whatever is beneath.
export const TOAST_BAND = "pt-16";
